_meta:
  model: workflows.Workflow
rows:
  - xref: takeout_import
    fields:
      name: Facebook takeout import
      description: Recognize an extracted Facebook export drive, confirm its channel, and import through the Facebook takeout owner.
      subject_declaration: storage.drive
      max_steps: 1000
      budget: {}
